<%= render "/shared/page_header_user" %>

<div class="container">
  <div class="row">
    <%= render partial: "/shared/summary_card", locals:{user: @user} %>
    <%= render partial: 'shared/profile_sidebar', locals: {cohorts: @admin_cohorts} %>
    <%= render partial: 'shared/cohort_membership', locals: {cohorts: @all_cohorts} %>
  </div>

 <button class="btn btn-outline-primary"><%= link_to 'Delete My Account',  "/users/profile",
   data: { confirm: 'Are you sure?' }, method: :delete, remote: true %></button>
</div>